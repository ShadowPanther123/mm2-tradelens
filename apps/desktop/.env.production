# Production builds remain offline unless both values are supplied together by
# the release environment. Never configure a snapshot URL without its trusted
# Ed25519 public key.
# VITE_SNAPSHOT_URL=https://values.example/v1/snapshot
# VITE_SNAPSHOT_PUBLIC_KEY=base64-public-key

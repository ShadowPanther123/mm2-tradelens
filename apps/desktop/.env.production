# Production values feed (baked into the build by Vite at compile time).
# The desktop app derives /v1/revision and /v1/signed-snapshot from this base
# URL and verifies every downloaded snapshot against the Ed25519 public key
# below. Both MUST be present together: a signed feed is required in production.
VITE_SNAPSHOT_URL=https://shadowpanther123.github.io/mm2-tradelens/v1/snapshot
VITE_SNAPSHOT_PUBLIC_KEY=6+NRTftGOce/nRdOQupWCmmfE3nJyIUXWF9Ew/BGKEc=
